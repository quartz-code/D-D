# Локальная конфигурация с ключом API — в репозиторий не попадает
config/config.json

# Рабочее состояние партии
state/*.json
state/*.jsonl
!state/.gitkeep

# Разложенная файловая система квеста (создаётся seed-скриптом)
/квест/
/complex-root/

__pycache__/
*.py[cod]
.venv/
